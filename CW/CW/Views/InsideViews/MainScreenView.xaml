<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CW.Views.InsideViews.MainScreenView">
    <Shell.TitleView>
        <StackLayout Orientation="Horizontal"
                     HorizontalOptions="FillAndExpand">
            <Button Text="Profile"
                    HorizontalOptions="EndAndExpand"
                    Command="{Binding OpenProfilePageCommand}"/>
        </StackLayout>
    </Shell.TitleView>
    <ContentPage.Content>
        <StackLayout BackgroundColor="White">
            <Label Text="Мои карты"
                   TextTransform="Uppercase"
                   VerticalOptions="Center"
                   Padding="20,10,0,10"
                   BackgroundColor="LightGray"/>
            <ListView ItemsSource="{Binding BankCards}"
                      HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
<<<<<<< HEAD
                                    <StackLayout.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OpenBankCardPageCommand}"
                                                              CommandParameter="{Binding}"/>
                                    </StackLayout.GestureRecognizers>
                                    <Label Text="{Binding Name}" 
                                   VerticalOptions="Start"
                                   Padding="10,0,0,10"/>
                                    <StackLayout Orientation="Horizontal" 
                                         VerticalOptions="EndAndExpand">
                                        <Label Text="{Binding ImgUrl}"/>
                                        <Label Text="{Binding Number}"/>
=======
                                    <StackLayout>
                                        <Label Text="{Binding Name}" 
                                               VerticalOptions="Start"
                                               Padding="10,5,0,0"/>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="{Binding ImgUrl}"
                                                   Padding="5,0,0,0"/>
                                            <Label Text="{Binding Number}"/>
                                        </StackLayout>
>>>>>>> 5dd3f8e... Modify view in main screen
                                    </StackLayout>
                                    <Label Text="{Binding Money}"
                                           HorizontalOptions="EndAndExpand"
                                           VerticalOptions="Center"
                                           Padding="0,0,10,0"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Label Text="Мои счета"
                   TextTransform="Uppercase"
                   VerticalOptions="Center"
                   Padding="20,10,0,10"
                   BackgroundColor="LightGray"/>
            <ListView ItemsSource="{Binding BankAccounts}"
                      HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
<<<<<<< HEAD
                                    <StackLayout.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OpenBankAccountPageCommand}"
                                                              CommandParameter="{Binding}"/>
                                    </StackLayout.GestureRecognizers>
                                    <Label Text="{Binding Name}" 
                                   VerticalOptions="Start"
                                   Padding="10,0,0,10"/>
                                    <StackLayout Orientation="Horizontal" 
                                         VerticalOptions="EndAndExpand">
                                        <Label Text="{Binding Number}"/>
=======
                                    <StackLayout>
                                        <Label Text="{Binding Name}" 
                                               VerticalOptions="Start"
                                               Padding="10,5,0,0"/>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="{Binding Number}"
                                                   Padding="5,0,0,0"/>
                                        </StackLayout>
>>>>>>> 5dd3f8e... Modify view in main screen
                                    </StackLayout>
                                    <Label Text="{Binding Money}"
                                           HorizontalOptions="EndAndExpand"
                                           VerticalOptions="Center"
                                           Padding="0,0,10,0"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Label Text="Мои кредиты"
                   TextTransform="Uppercase"
                   VerticalOptions="Center"
                   Padding="20,10,0,10"
                   BackgroundColor="LightGray"/>
            <ListView ItemsSource="{Binding BankCredits}"
                      HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal">
                                    <StackLayout>
                                        <Label Text="{Binding Name}" 
                                               VerticalOptions="Start"
                                               Padding="10,5,0,0"/>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="{Binding PaymentInfo}"
                                                   Padding="10,0,0,0"/>
                                            <Label Text="{Binding Number}"/>
                                        </StackLayout>
                                    </StackLayout>
                                    <Label Text="{Binding Money}"
                                           HorizontalOptions="EndAndExpand"
                                           VerticalOptions="Center"
                                           Padding="0,0,10,0"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>